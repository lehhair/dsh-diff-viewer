//#region src/index.ts
/** Host loader entry for the browser-only diff-viewer plugin. */
/** Provides no host-side behavior (the surface is entirely browser-side). */
function apply() {}
//#endregion
export { apply };
